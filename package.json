{
  "name": "health-clinic",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "npm run start --prefix backend",
    "server": "cd backend && npm start",
    "build": "npm run build --prefix frontend",
    "client": "cd frontend && npm start",
    "startproject": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
